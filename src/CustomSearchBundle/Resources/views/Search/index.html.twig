{% extends 'eZDemoBundle::pagelayout.html.twig' %}

{% block content %}
    <style>
        #search_contentTypes {
            height:150px;
            width:250px;
            padding: 10px;
        }
        #search_searchQuery {
            top:0px;
            width:350px;
            margin-bottom: 15px;
        }
        #searchTips_ {
            width: 350px;
        }
    </style>
    <h1>Custom eZ Search</h1>
    <p>Search for anything here...</p>

    {{ form_start(form) }}<br>
        <table style="margin: 0 auto; ">
            <trpadding-right:60px;>
                <td style="padding-right:60px;vertical-align:top;">
                    <strong>Search</strong>
                    {{ form_row(form.searchQuery) }}
                    <div id="searchTips_">
                        <p>
                            Search for some text within an article, blog, or on any other
                            content type available.
                        </p>
                        <p>
                            If the search results are not what you were expecting,
                            check spelling of keywords, try changing some keywords or
                            try searching with less specific keywords.
                            Also it might help to reduce number of keywords to get more results.

                        </p>
                    </div>
                </td>
                <td style="padding-right:60px;vertical-align:top;width:350px;"><strong>Select your filter(s)</strong>{{ form_row(form.contentTypes) }}</td>
                <td style="padding-right:60px;width:320px;">{{ form_widget(form.save) }}</td>
            </tr>
        </table>
    {{ form_end(form) }}
<br>
{% endblock %}